# Environment variables for subdomain-based admin access

# Admin subdomain configuration
# For development (use admin.localhost:3000)
ADMIN_SUBDOMAIN=admin

# For production, use an obscured subdomain like:
# ADMIN_SUBDOMAIN=admin-k8j2m9x1

# Client-side environment variables (must be prefixed with NEXT_PUBLIC_)
NEXT_PUBLIC_ADMIN_SUBDOMAIN=admin

# For production:
# NEXT_PUBLIC_ADMIN_SUBDOMAIN=admin-k8j2m9x1